import React from 'react';
import { CartProps } from './Cart.props';

const Cart = (props: CartProps) => {
  return (
    <div className='cart'>
      <h2>Cart</h2>
    </div>
  );
};

export default Cart;